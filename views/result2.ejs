<%- include('partials/header2') %>

<div class="container rounded bg-white mt-5 mb-5" id="box">
    <div class="row">
        <div class="col-md-3 border-right">
            
        </div>
        <div class="col-md-7 border-right">
            <div class="p-3 py-5">
                <div class="d-flex justify-content-between  align-items-center mb-3">
                    <h4 class="text-right mx-auto">View Result</h4>
                </div>
                <div class="row mt-2">
                    <img src="images/new_logo-2.png" alt="">
                    <ol>
                        <% for (let i = 0; i < resultItems.length; i++){ %>
                            <li><%= resultItems[i].courseCode %><p class="ol-p"><%= resultItems[i].courseTitle %></p><p class="ol-p2"><%= resultItems[i].courseGrade %></p></li>
                        <% } %>
                    </ol>
                </div>
            </div>
        </div>
    </div>
  </div>
  </div>
  </div>

<%- include('partials/footer') %>
